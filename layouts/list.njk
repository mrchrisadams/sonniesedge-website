{% extends "default.njk" %}
{% block list %}
    <ul>
    {% for post in posts %}
        <li>{{ post.date }} - <a href="{{ post.path }}">{{ post.title }}</a></li>
    {% endfor %}
    </ul>
{% endblock %}